<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./styles/style.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous" />
    <title>GPU Blog</title>
  </head>

  <body>
    <%- include('./partials/header.ejs') %>
    <div class="parent-post-container">
      <% if(locals.posts && posts.length!=0) { %> <% for(let i=0; i <
      posts.length; i++) { %>
      <div class="post-container">
        <a href="<%-posts[i].url%>" style="text-decoration: none"
          ><div class="post-content"><%-posts[i].title%></div>
        </a>
      </div>
      <% } %> <%} else{%>
      <div id="post-alternative">
        It's a ghost town 👻—no posts to see here! But keep scrolling! ⏳✨
      </div>
      <%}%>
    </div>
    <%- include("./partials/footer.ejs") %>
  </body>
</html>
